#define MYLIB_GIT_COMMIT      "${GIT_COMMIT}"
#define MYLIB_GIT_COMMIT_LONG "${GIT_COMMIT_LONG}"
#define MYLIB_GIT_COMMIT_DATE "${GIT_COMMIT_DATE}"
#define MYLIB_GIT_BRANCH      "${GIT_BRANCH}"
#define MYLIB_GIT_TAG         "${GIT_TAG}"
#define MYLIB_GIT_TAG_LONG    "${GIT_TAG_LONG}"
#define MYLIB_GIT_CUSTOM      "${GIT_TAG}-${GIT_COMMIT}"

#define MYLIB_VERSION       "${VERSION}"
#define MYLIB_VERSION_MAJOR ${VERSION_MAJOR}
#define MYLIB_VERSION_MINOR ${VERSION_MINOR}
#define MYLIB_VERSION_ALTER ${VERSION_ALTER}
#define MYLIB_VERSION_BUILD "${VERSION_BUILD}"
